// 1. Write a function called capitalize() that takes a string and uses.
// map to return the same string in all caps.


const capitalize = (str => str)
capitalize.map(char => char.toUpperCase())
console.log('aadsdsddf');